# 本地开发默认不依赖内网；需要真实后端时再填写
VITE_SERVICE_BASE_URL=http://localhost:8080/api/v2

# 是否开启 mock 服务，Y | N
VITE_SERVICE_MOCK=N

# other backend service base url
VITE_OTHER_SERVICE_BASE_URL= `{
  "demo": "http://localhost:7589/api/v2"
}`
