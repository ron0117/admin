# 列表 CRUD 页自测清单模板

复制并替换 `{模块}`、`{页面}`、`{Tab}`、接口 path。

---

## 一、权限（本模板）

- [ ] N/A：真实 privilege 已 stub；若演示 `v-auth`，确认仍显示（全通）

## 二、路由与 PageMenu

- [ ] 访问 `/{module}/{page}` 正常
- [ ] Tab（若有）可切换且内容为搜索+表格
- [ ] 刷新后默认 Tab 与列表正常

## 三、列表与查询

- [ ] 默认 list 含分页参数
- [ ] 筛选/重置/刷新正确

## 四、分页

- [ ] 翻页、改 pageSize 后数据与 total 正确

## 五、新增 / 编辑 / 删除

- [ ] 校验、提交字段、成功后刷新列表
- [ ] 删除有确认；取消不删

## 六、国际化

- [ ] 至少简体中文菜单、表头、弹窗文案正确

## 七、接口速查

| 能力 | 方法 | Path |
|------|------|------|
| 列表 | | `{list-path}` |
| 创建 | | `{create-path}` |
| 更新 | | `{update-path}` |
| 删除 | | `{delete-path}` |
