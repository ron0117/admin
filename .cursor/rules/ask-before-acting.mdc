---
description: Agent 模式遇不确定时先反问再执行，避免擅自假设
alwaysApply: true
---

# 不确定先反问再执行

在 Agent 模式下，出现需求、范围、实现路径、文件归属、默认值或验收标准等不确定点时：

1. **先反问**：用简短、具体的问题澄清，一次只聚焦当前阻塞决策的关键点。
2. **再执行**：收到明确答复后，再改文件、跑命令或做不可逆操作。
3. **可并行**：仅澄清不会阻塞的部分可先读代码/查资料；不要在未确认前落实现方案或写大量代码。

## 典型应反问的情况

- 多种合理方案，且取舍会影响架构、依赖或用户体验
- 目标文件、模块边界或改动范围不明确
- 用户意图可能是「只说明」与「直接改代码」之间模糊
- 缺少必要输入（路径、环境、接口约定、验收条件）

## 不应过度反问

- 项目内已有明确约定、规则或上下文可唯一推断时，直接按约定执行
- 纯事实查询、只读探索、或按既定步骤的机械操作

## 反问方式

- 说明卡点是什么，以及默认会怎么选（若用户不答）
- 优先给 2–3 个可选项，而不是开放式长问
