---
description: 按 docs 四步流水线（梳理版→技术设计→开发计划→自测方案）逐步产出文档；不写业务代码
globs: docs/requirements/**/*.md,docs/detail/**/*.md,docs/design/**/*.md,docs/plan/**/*.md,docs/test/**/*.md
alwaysApply: false
---

# 需求文档流水线（逐步推进）

当用户基于 `docs/requirements` 做需求梳理，或要求进入/继续文档流水线时，按下列顺序**逐步**产出文档。默认一次只完成当前步骤；完成后再询问是否进入下一步。

## 流水线与命名

源需求：`docs/requirements/需求-{名称}.md`  
`{名称}` = 源文件名中「需求-」与「.md」之间的部分（例：`需求-框架构想.md` → `{名称}` = `框架构想`）。

| 步骤 | 产出路径 | 职责 |
|------|----------|------|
| 1 | `docs/detail/需求-{名称}-梳理版.md` | 需求拆解、边界、优先级、验收、待澄清 |
| 2 | `docs/design/技术设计-{名称}.md` | 架构与模块设计、关键接口/数据、可行性 |
| 3 | `docs/plan/开发计划-{名称}.md` | 分期、任务拆分、依赖与排期建议 |
| 4 | `docs/test/自测方案-{名称}.md` | 自测范围、用例、步骤与通过标准 |

- 目录不存在则创建对应目录。
- 目标已存在：说明将覆盖/更新，征得确认后再写（用户明确要求覆盖除外）。
- 仅给主题未给路径时：在 `docs/requirements/` 匹配 `需求-*.md`；多候选先反问。
- 历史命名 `docs/detail/需求梳理-{名称}.md` 视为旧路径；若存在且新路径不存在，迁移/重命名为 `需求-{名称}-梳理版.md` 并更新文内链接后再继续。

## 逐步规则

1. **严格按 1 → 2 → 3 → 4**；不得跳步批量生成（除非用户明确要求「一次出齐」）。
2. 进入第 N 步前：通读源需求 + 已完成的前序文档，保持 `{名称}` 与结论一致。
3. 每步结束后：给出产出路径与简短摘要，并询问是否进入下一步。
4. 用户指定某一步（如「写技术设计」）时：检查前序是否已有；缺失则先反问补做还是仅基于源需求继续。

## 允许与禁止

**允许：** 生成/更新上表四类文档；列出待澄清项；在文档间写清映射与链接。

**禁止：** 编写/修改应用代码、依赖、配置、脚手架；以文档任务名义做功能开发或重构。  
实现代码须用户另开明确开发任务。

## 各步内容要点

### 步骤 1 · 需求梳理版

建议章节：产品定位；用户与用例（Must/Should/Could + ID + 验收要点）；功能拆解；非功能需求；端到端流程；范围 In/Out；MoSCoW；待澄清；验收标准；文档映射（指向本流水线四份文档 + 源需求）。

文风：可评审、可排期、可验收；表格优先；勿整篇粘贴源需求。

### 步骤 2 · 技术设计

建议章节：目标与约束；总体架构；模块职责；关键流程；数据模型；接口/IPC；技术选型与备选；风险与对策；与梳理版用例/验收的追溯。

### 步骤 3 · 开发计划

建议章节：里程碑；任务拆分（可勾选）；依赖顺序；人力/并行建议；MVP 范围对齐梳理版；风险与缓冲。

### 步骤 4 · 自测方案

建议章节：测试范围；环境与前置；用例表（步骤/期望）；对应用例/验收 ID；通过/失败标准；回归要点。

## 文档映射（文末建议统一）

| 文档 | 路径 |
|------|------|
| 原始需求 | `docs/requirements/需求-{名称}.md` |
| 需求梳理版 | `docs/detail/需求-{名称}-梳理版.md` |
| 技术设计 | `docs/design/技术设计-{名称}.md` |
| 开发计划 | `docs/plan/开发计划-{名称}.md` |
| 自测方案 | `docs/test/自测方案-{名称}.md` |
