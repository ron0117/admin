---
description: 代码审查与逻辑检查时的排除范围
globs:
alwaysApply: true
---

# 代码审查排除范围

进行代码逻辑检查、代码审查、静态分析时，请**不要**分析或修改以下内容：

- **`src/_apis/`**（若存在）：Swagger 生成物，不纳入业务逻辑审查。
- **`node_modules/`**：第三方依赖，仅作引用，不审查其内部实现。
- **生成物**：如 `src/router/elegant/*`、`src/typings/elegant-router.d.ts`、`src/typings/components.d.ts`（由工具生成；审查时关注调用方，勿手改后当业务源）。

只针对项目内自研代码（如 `src/service`、`src/views`、`src/components`、`src/store`、`docs/` 约定相关变更）进行分析和检查。
