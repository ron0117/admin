---
description: 通用编码风格与文件组织
globs: ["**/*.ts", "**/*.vue", "**/*.tsx"]
alwaysApply: false
---

# 编码风格（通用）

- **不可变性**：尽量不原地修改对象，返回新副本
- **文件组织**：按功能/领域划分；单文件建议 200–400 行，上限约 800 行；高内聚、低耦合
- **禁止**：在生产代码中使用 `console.log`
- **命名**：可读、表意清晰的标识符；函数保持短小（建议 <50 行），避免深层嵌套（>4 层）
