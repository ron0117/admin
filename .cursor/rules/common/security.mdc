---
description: 安全与输入校验
globs: ["**/*.ts", "**/*.vue"]
alwaysApply: false
---

# 安全规范

- **密钥**：禁止硬编码 API Key、密码、Token；使用环境变量或密钥管理
- **用户输入**：在系统边界做校验（可用 `src/constants/reg.ts`）；不信任外部数据，失败快速返回明确错误
- **XSS**：禁止直接使用 `innerHTML`；若必须用 `v-html`，须配合 dompurify
- **权限**：本模板 stub 非安全边界；对接真实后端时须由服务端鉴权
- **SQL**：若涉及后端协作，使用参数化查询，防止注入
