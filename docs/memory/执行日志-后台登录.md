# 执行日志 · 后台登录

## 执行记录 2026-09-14 16:20

- **阶段**：按方案 1 实现（独立密钥 + 独立 `/admin/auth`）
- **计划文档**：用户选定方案 1；产品说明见 `docs/requirements/需求-后台登录.md`，用户管理设计/自测已同步
- **完成任务**：Nest `admin-auth` 模块；`/admin/users*` 改 `JwtAdminAuthGuard`；Admin 登录/刷新/登出改打 `/admin/auth/*`
- **实现摘要**：后台 JWT 使用 `JWT_ADMIN_SECRET` + `aud=admin`；refresh 哈希前缀 `admin:`。Electron 仍走 `/auth/*` + `JWT_SECRET`。两边 token 互验失败。
- **涉及文件**：`nestjs/apps/nest-api/src/admin-auth/**`、`admin-users/admin-users.controller.ts`、`test/app.e2e-spec.ts`；`admin/src/service/api/auth.ts`、`service/request/nest.ts`
- **验证**：Nest `npm run test:e2e` 通过（含后台 JWT 隔离）；Admin `pnpm typecheck` 通过。需重启已在跑的 nest-api 以加载 `JWT_ADMIN_SECRET`。
- **Commit**：已提示用户提交（建议：`feat: 后台登录与 Electron JWT 隔离`）
---
