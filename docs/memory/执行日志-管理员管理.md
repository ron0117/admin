# 执行日志 · 管理员管理

## 执行记录 2026-09-15

- **阶段**：按开发计划实现
- **计划文档**：`docs/plan/开发计划-管理员管理.md`
- **完成任务**：N0.1–N0.6，N1.1–N1.6，N2.1–N2.4，A3.1–A3.5，T4.1–T4.2
- **实现摘要**：
  - Nest：新增 `AdminUser` / `AdminRefreshToken` 迁移；`/admin/auth` 只认管理员表；新模块 `/admin/admins*`；`/admin/users*` 去掉 role 与禁自己/LAST_ADMIN；seed 只 upsert `AdminUser`
  - Admin：新页 `views/manage/admin`；用户管理去角色；登录映射不再依赖 `user.role`；路由 `manage_admin` + i18n
- **涉及文件（主要）**：
  - `nestjs/apps/nest-api/prisma/schema.prisma`、`migrations/20260915090000_admin_user_separate`、`seed.ts`
  - `nestjs/.../admin-auth/**`、`admins/**`、`admin-users/**`、`test/app.e2e-spec.ts`
  - `admin/src/views/manage/admin/index.vue`、`manage/user/index.vue`、`service/api/admins.ts`、`auth.ts`、`admin-users.ts`
- **验证**：`nestjs/apps/nest-api` `npm run test:e2e` 通过；迁移已 deploy 且 seed 成功；`admin` `pnpm typecheck` 通过。T4.3 需本地 `VITE_AUTH_MOCK=N` 人工联调。
- **注意**：升级后须重新 seed；旧后台 JWT（sub 为 User.id）全部失效，需用 AdminUser 重新登录。
---
