后台需要做用户管理，范围按完整 CRUD：

- 分页列表，支持按邮箱/用户名、状态、角色筛选
- 后台创建用户
- 编辑资料（用户名、角色等）
- 启用 / 禁用
- 重置密码

数据与接口以 NestJS 现有 `User` 为准（邮箱、用户名、状态 `pending_verification | active | disabled`、角色 `user | admin`）。
后台菜单与按钮权限仍保持模板 stub 全通，本期不做 RBAC。
先走文档流水线，暂不写代码。
