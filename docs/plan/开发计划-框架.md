# 开发计划 · 框架

> 对齐：[需求-框架-梳理版](../detail/需求-框架-梳理版.md)、[技术设计-框架](../design/技术设计-框架.md)  
> MVP 范围：Must（UC-01～07 / AC-01～06）+ README（AC-07）  
> 粗估合计：**4～6 人天**（单人串行）；缓冲见 §6  
> **执行状态（2026-09-11）：Must 主体已完成；见 §2 勾选与 §9 残留项**

---

## 1. 里程碑

| 里程碑 | 目标 | 建议耗时 | 完成标志 | 状态 |
|--------|------|----------|----------|------|
| M0 脚手架就位 | 源码落入目标仓，可 `pnpm i` | 0.5～1d | 依赖安装成功；无 `_apis`/业务 secrets | **完成** |
| M1 业务剥离 | 无 O2O 业务域与业务 API | 1～1.5d | AC-03 目录层面满足 | **完成**（T1.6 部分残留） |
| M2 权限 stub | 菜单/路由/PageMenu/指令全通 | 1～1.5d | AC-04；无半删症状 | **完成** |
| M3 可冒烟 | mock 登录 + home + demo | 0.5～1d | AC-02/05/06；`pnpm dev` 可用 | **完成**（完整 UI 冒烟建议人工复核） |
| M4 文档收尾 | README + 品牌/env 脱钩 | 0.5d | AC-07；Should 项尽量完成 | **完成** |

**建议顺序（不可打乱依赖）：** M0 → M1 → M2 → M3 → M4。  
M1 与「删 CI/docker」可在 M0 末尾并行；**M2 必须在 M1 后**（避免残留业务页掩盖 stub 问题）。

---

## 2. 任务拆分（可勾选）

### M0 · 脚手架复制（UC-01）

- [x] T0.1 从 `D:\项目\o2o\code\o2o-backoffice` 复制到 `d:\模板\admin`（保留已有 `docs/`、`.cursor/`）
- [x] T0.2 排除：`node_modules`、`dist`、`.temp`、`src/_apis`、锁文件按需保留或重装
- [x] T0.3 不带入：`azure-pipelines*`、`docker/`、原上传脚本（Q-05）
- [x] T0.4 改 `package.json` name（暂 `admin` / Q-06）；清理 `openAi*` 脚本或改占位
- [x] T0.5 `pnpm i` 成功

### M1 · 剥离业务（UC-06）

- [x] T1.1 删除 views：`announce` `bo` `cash` `game` `member` `system` `user-center`
- [x] T1.2 删除业务 API（保留 `auth`/`route`/`upload`/`index` 并裁剪导出）
- [x] T1.3 删除 `roleConfig.ts`、`constants/privilege/**`、`PRIV` 引用面
- [x] T1.4 删除 `store/modules/role`、privilege-tree 工具、业务 constants/enum
- [x] T1.5 删除业务耦合控件（`member-input`、`game-category-select` 等，Q-04）
- [x] T1.6 清理业务 locales / 无用 import；修正 `components/index` 若断链  
  - **残留**：`src/locales/langs/pages/{bo,game,member,cash,system,announce,manage}.ts` 仍为 i18n Schema 类型占位，未删净；`route` 段已去掉业务 key
- [x] T1.7 跑 `pnpm gen-route`，确认 elegant 生成物无已删路由  
  - 说明：交互式 `sa gen-route` 未用；由 `pnpm dev` 触发 elegant-router 重生成，现仅 `home`/`demo`/`_builtin`

### M2 · Privilege Stub（UC-05）

- [x] T2.1 stub `hooks/business/role.ts`（`judgePrivilege` / `hasMenuPrivilege` 恒 true）
- [x] T2.2 改造 `router/guard/route.ts`：仅登录校验，去掉 privilege 403
- [x] T2.3 改造 `store/modules/route/shared.ts`：菜单/路由不过滤
- [x] T2.4 改造 `page-menu.vue`：去掉 privilege Tab 过滤
- [x] T2.5 stub `directives/auth.ts`、`hie.ts`（Q-02）
- [x] T2.6 精简 `auth` store 对 privileges / role 的硬依赖
- [x] T2.7 全文检索确认无必崩引用（`PRIV.`、`roleConfig`、`bo-role` 等）

### M3 · 登录 Mock + 示例（UC-03/04/07）

- [x] T3.1 清洗 `.env*`：去内网默认；开启 mock/旁路相关开关
- [x] T3.2 `auth.ts`（及 auth store 登录流）改为 mock 可走通
- [x] T3.3 精简保留 `views/home`
- [x] T3.4 新增 `views/demo/**`：PageMenu + 至少一种 JQ*（静态数据）
- [x] T3.5 `pnpm gen-route` + `pnpm dev` 冒烟：登录 → 首页 → demo → 多标签/主题  
  - **说明**：dev 已起过、关键模块 200；**完整浏览器路径建议人工再点一遍**（dev 进程后续已中止）
- [x] T3.6 尽量通过 `pnpm typecheck`（阻塞项优先修；非阻塞记入已知问题）  
  - 已通过（修复 `data-table` / `menu-item` / `ui.ts` / `hooks/common/table` 与 EP 类型兼容）

### M4 · 文档与品牌（UC-08、AC-07）

- [x] T4.1 更新 README：如何安装、加页、请求约定、权限已关闭、mock 说明
- [x] T4.2 应用标题 / logo 文案与 O2O 脱钩（Should）（`VITE_APP_TITLE`、`system.title` → Admin）
- [x] T4.3 对照 AC-01～07 自检清单勾选（见下表）

#### AC 自检（T4.3）

| ID | 标准 | 结果 |
|----|------|------|
| AC-01 | monorepo 可安装，含 `packages/@sa/*` | 通过 |
| AC-02 | `pnpm dev` 可打开，布局壳可见 | 通过（建议人工复核） |
| AC-03 | 无业务域 views/API；无 `roleConfig`/`PRIV` 配置树 | 通过 |
| AC-04 | 路由/菜单不因 privilege 不可用 | 通过（stub） |
| AC-05 | 文件约定加页 / elegant 可用 | 通过（demo 已生成） |
| AC-06 | 请求封装 + 约定组件可在示例使用 | 通过（demo） |
| AC-07 | README 说明保留/删除/加页/权限关闭 | 通过 |

### 不在本计划（Won't / Could 另开）

- 新 RBAC 实现、依赖大规模裁剪（UC-10）、生产 CI/CD、以 `code_component` 为主源

---

## 3. 依赖顺序

```text
T0.* ──► T1.* ──► T2.* ──► T3.* ──► T4.*
              │
              └── T1.7 gen-route 可在 T1 末与 T2 前强制执行一次
              └── T3.5 依赖 T2 完成（否则菜单/403 干扰冒烟）
```

| 可并行 | 说明 |
|--------|------|
| T0.3 与 T0.4 | 复制后同时改名与剔 CI |
| T1.1～T1.5 | 不同目录删除可并行（注意最后统一 gen-route） |
| T2.1～T2.5 | 文件级可并行，T2.7 必须最后串行验收 |
| T4.* | 可在 T3.5 冒烟通过后立即开始 |

---

## 4. 人力与排期建议

| 模式 | 建议 |
|------|------|
| 单人 | 按 M0→M4 串行，约 4～6 人天 |
| 双人 | A：M0+M1+M3 业务/示例；B：M2 stub + env/auth mock；在 T3.5 前合并联调 |
| 日节奏示例 | D1: M0+M1 大半；D2: M1 收尾+M2；D3: M3；D4: typecheck+M4+缓冲 |

角色假设：熟悉 Vue3 + 该 Soybean 二次定制者效率按上表；陌生者建议 +30%～50%。

---

## 5. MVP 对齐

| 梳理版 | 计划任务 | 里程碑 | 状态 |
|--------|----------|--------|------|
| UC-01 / AC-01 | T0.* | M0 | 完成 |
| UC-06 / AC-03 | T1.* | M1 | 完成 |
| UC-05 / AC-04 | T2.* | M2 | 完成 |
| UC-02 / AC-05 | T1.7、T3.4～T3.5 | M1/M3 | 完成 |
| UC-03/04 / AC-06 | T3.4 | M3 | 完成 |
| UC-07 / AC-02 | T3.1～T3.5 | M3 | 完成 |
| AC-07 / NFR-04 | T4.1 | M4 | 完成 |
| UC-08/09 Should | T3.1～T3.2、T4.2 | M3/M4 | 完成 |
| UC-10 Could | 不排期 | — | — |

---

## 6. 风险与缓冲

| 风险 | 缓冲 | 应对 |
|------|------|------|
| privilege 遗漏导致空菜单/403 | +0.5d | T2.7 检索清单；对照技术设计 §3.4 |
| 删业务后类型/路由大面积报错 | +0.5～1d | 先 gen-route；demo 用静态数据；typecheck 分批修 |
| mock 登录与 storage/guard 细节不符 | +0.5d | 优先静态超管旁路打通再细化 mock |
| 复制时覆盖目标仓已有 `docs/` | — | 复制策略：合并而非整目录覆盖 docs |

**计划缓冲合计：** 建议在 4～6 人天外预留 **+1 人天** 再对外承诺。

---

## 7. 进入开发的前置确认

实施代码前建议用户确认（未答则用梳理版默认）：

| ID | 默认 | 实际 |
|----|------|------|
| Q-01 登录 | mock + 旁路 | 已采用（`VITE_AUTH_MOCK=Y`） |
| Q-02 指令 | 保留 stub | 已采用 |
| Q-03 示例 | home + demo | 已采用 |
| Q-04 业务控件 | 不迁 | 已采用 |
| Q-05 CI/docker | 不带入 | 已采用 |
| Q-06 命名 | `admin` | 已采用 |

---

## 8. 文档映射

| 文档 | 路径 |
|------|------|
| 原始需求 | [docs/requirements/需求-框架.md](../requirements/需求-框架.md) |
| 需求梳理版 | [docs/detail/需求-框架-梳理版.md](../detail/需求-框架-梳理版.md) |
| 技术设计 | [docs/design/技术设计-框架.md](../design/技术设计-框架.md) |
| 开发计划（本文） | `docs/plan/开发计划-框架.md` |
| 自测方案 | [docs/test/自测方案-框架.md](../test/自测方案-框架.md) |

---

## 9. 残留 / 可选后续

| 项 | 说明 | 优先级 |
|----|------|--------|
| 业务 locales 占位文件 | `langs/pages` 下 bo/game/… 仍在，仅为 Schema；可后续抽空 Schema 再删 | Could |
| 人工完整冒烟 | 登录 → home → demo Tab → 多标签/主题（对照自测方案 Must 用例） | Should |
| 依赖裁剪 / 新 RBAC | 原 Won't/Could，另开需求 | — |
